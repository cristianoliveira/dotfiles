variables:
  - name: tag_from
    default: HEAD
  - name: tag_to
    default: HEAD~1

steps:
  - .role git-commit
  - .file `bash ~/.dotfiles/aichat/scripts/git-append-context.sh {{tag_from}} {{tag_to}}`
