variables:
  - name: tag_from
  - name: tag_to
steps:
  - .role doc-release-man
  # Attach commit list (newest first) with trailers if present
  - .file `git -c log.showSignature=false log {{tag_from}}..{{tag_to}} --pretty=format:'%H|%an|%ad|%s%n%b' --date=short`

