variables:
  - name: args
    rest: true
    default: "no more context"

steps:
  - .role git-commit
  - .file `git diff --cached` -- write a semantic git commit message using this context -- {{args}}
