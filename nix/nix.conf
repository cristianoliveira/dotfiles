# Based on the default nix.conf generated by nix-darwin
allowed-users = *
auto-optimise-store = false
build-users-group = nixbld
builders = 
cores = 0
extra-experimental-features = nix-command flakes
extra-sandbox-paths = 
max-jobs = auto
require-sigs = true
sandbox = false
sandbox-fallback = false
